# =============================================================================
# WORLDVIEW — Python requirements
# =============================================================================

# --- Analysis scripts (analysis/python/) ---
pandas>=1.5
numpy>=1.23
scikit-learn>=1.2
scipy>=1.10
matplotlib>=3.6
seaborn>=0.12
adjustText>=0.8
nltk>=3.8
statsmodels>=0.14

# --- Preprocessing: SBERT embeddings ---
sentence-transformers>=2.2

# --- Preprocessing: CLIP embeddings ---
# torch>=2.0                # install separately for your CUDA version
# torchvision>=0.15
open-clip-torch>=2.20
Pillow>=9.0
tqdm>=4.64

# --- Preprocessing: VQA descriptions (uses local Ollama) ---
requests>=2.28

# --- Preprocessing: ablation image generation (uses Replicate API) ---
replicate>=0.20

# =============================================================================
# R requirements (analysis/R/)
# =============================================================================
# install.packages(c(
#   "tidyverse",      # dplyr, ggplot2, tidyr, readr, stringr, purrr
#   "textstem",       # lemmatization
#   "text2vec",       # TF-IDF, DTM
#   "tidytext",       # tidy text mining (ablation script)
#   "ggrepel",        # non-overlapping text labels
#   "stopwords",      # English stopwords (Snowball)
#   "patchwork",      # combining ggplot panels (ablation script)
#   "scales"          # percent_format() etc.
# ))
